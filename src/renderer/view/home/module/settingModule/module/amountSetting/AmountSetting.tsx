import React from 'react';
import PropTypes from 'prop-types';

const AmountSetting = props => {
    return (
        <div className='amount-setting-container'>
            对冲金额设置
        </div>
    );
};

AmountSetting.propTypes = {
    
};

export default AmountSetting;